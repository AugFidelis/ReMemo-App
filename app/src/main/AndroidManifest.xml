<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/rememo_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/rememo_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ReMemo">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ReMemo"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.screens.alarms.AlarmListScreen"
            android:exported="false"
            android:label="@string/title_activity_alarm_list_screen"
            android:theme="@style/Theme.ReMemo"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".ui.screens.reminders.ReminderListScreen"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".ui.screens.reminders.ReminderListActivity"
            android:exported="false"
            android:label="@string/title_activity_reminder_list"
            android:theme="@style/Theme.ReMemo"
            android:windowSoftInputMode="adjustResize" />
    </application>

</manifest>